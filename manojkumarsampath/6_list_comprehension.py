#6. Create a list comprehension that generates a list of squares of
# numbers from 1 to 20.

squares = [x**2 for x in range(1, 21)]
print(f"list comprehension that generates from 1 to  20 is {squares}")
